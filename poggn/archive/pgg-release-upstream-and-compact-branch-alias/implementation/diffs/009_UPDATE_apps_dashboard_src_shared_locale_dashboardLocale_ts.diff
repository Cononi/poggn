diff --git a/apps/dashboard/src/shared/locale/dashboardLocale.ts b/apps/dashboard/src/shared/locale/dashboardLocale.ts
index 0c72200..416c437 100644
--- a/apps/dashboard/src/shared/locale/dashboardLocale.ts
+++ b/apps/dashboard/src/shared/locale/dashboardLocale.ts
@@ -72,6 +72,18 @@ export const dashboardLocale = {
     topicStage: "stage",
     topicStatus: "status",
     archiveType: "archive type",
+    versionBump: "version bump",
+    targetVersion: "target version",
+    workingBranch: "working branch",
+    releaseBranch: "release branch",
+    publishStatus: "publish status",
+    publishMode: "publish mode",
+    upstreamStatus: "upstream status",
+    cleanupStatus: "cleanup status",
+    cleanupReason: "cleanup reason",
+    cleanupTiming: "cleanup timing",
+    releaseReview: "Release Review",
+    releaseReviewHint: "dashboard에서 release branch를 확인한 뒤 사용자가 main merge와 release 삭제를 진행합니다.",
     archivedAt: "archived",
     artifactCompleteness: "artifact completeness",
     markdown: "markdown",
@@ -154,6 +166,18 @@ export const dashboardLocale = {
     topicStage: "stage",
     topicStatus: "status",
     archiveType: "archive type",
+    versionBump: "version bump",
+    targetVersion: "target version",
+    workingBranch: "working branch",
+    releaseBranch: "release branch",
+    publishStatus: "publish status",
+    publishMode: "publish mode",
+    upstreamStatus: "upstream status",
+    cleanupStatus: "cleanup status",
+    cleanupReason: "cleanup reason",
+    cleanupTiming: "cleanup timing",
+    releaseReview: "Release Review",
+    releaseReviewHint: "Review the release branch here, then merge to main and delete the release branch from the dashboard follow-up flow.",
     archivedAt: "archived",
     artifactCompleteness: "artifact completeness",
     markdown: "markdown",
