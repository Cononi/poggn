---
pgg:
  topic: "dashboard-workflow-overview-sync"
  stage: "review"
  status: "reviewed"
  skill: "pgg-code"
  score: 96
  updated_at: "2026-04-24T16:59:44Z"
---

# code.review

## Expert Notes

| Expert | Score | Summary | Blocking |
|---|---:|---|---|
| 시니어 백엔드 엔지니어 | 96 | core snapshot이 `state/history.ndjson`를 `historyEvents`로 제공하고 workflow node detail timestamp metadata를 보존하도록 구현했다. dashboard model은 completed/update/start time을 분리하고 broad topic fallback을 완료 시각으로 직접 복제하지 않는다. | none |
| 테크 리드 | 96 | revision status, telemetry ingestion, compact UI, i18n 변경이 각각 core/shared/model/UI 경계에 배치되어 있다. pgg stage 이름과 workflow 순서는 유지했고, React Flow status도 `revising`을 같은 source로 인식한다. | none |
| 코드 리뷰어 | 96 | bordered time/status box 제거, small caption, tooltip, visible overflow safe area, reduced-motion fallback 유지가 구현됐다. build 검증은 통과했으며 visual parity는 QA에서 브라우저로 확인해야 한다. | none |

## Findings

- blocking issues: none

## Verification

- `pnpm --filter @pgg/dashboard build`: pass
- `pnpm --filter @pgg/core build`: pass
- source check for telemetry/status/tooltip keys: pass
- source check for removed bordered time/status box pattern: pass

## Residual Risks

- visual size and connector alignment still need QA browser/screenshot verification against `add-img/5.png` and `add-img/1.png`.
- dashboard static data must be regenerated by the normal dashboard data flow before existing static snapshots include `historyEvents`.
